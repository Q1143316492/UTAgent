# archive — 已归档的面板 golden（勿回日常）

曾用于一次性冒烟的预写整页拼装脚本（E08/E14/E15 及配套 count assert）。

- **正式拼 UI** = `utagent chat`（L2：C02/C14/C15…）
- **禁止**把本目录脚本重新写入 `suite_map.json` 的 `daily_l1`
- 若需手工复现配方：`utagent exec --file archive/<script>.py`（不计入日常验收）
