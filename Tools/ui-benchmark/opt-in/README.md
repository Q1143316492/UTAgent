# opt-in — 按需 / 临时验证

**不是**日常回归，**不是**永久归档。

- 改 before-exec / after-tool / 消息层时：`-Cases E09` 或 `-L2Only -Cases C11` 等。
- 用例稳定且日常已覆盖 → **删脚本**，真源表标已删。
- 只有仍需按名抽查的钩子用例才短期留在这里。

映射见上级 `suite_map.json` 与 `run_benchmark.ps1`。
